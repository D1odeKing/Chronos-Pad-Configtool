# Peg RGB Matrix configuration (auto-generated)
rgb = Rgb_matrix(ledDisplay=Rgb_matrix_data(keys=[Color.WHITE]*4, underglow=[Color.OFF]*0))
keyboard.extensions.append(rgb)

# Extra user snippet:
# Peg RGB Matrix configuration (auto-generated)
rgb = Rgb_matrix(ledDisplay=Rgb_matrix_data(keys=[Color.WHITE]*4, underglow=[Color.OFF]*0))
keyboard.extensions.append(rgb)